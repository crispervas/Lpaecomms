<%# ── MASHUP 1 — Products + Location ───────────────────────────────
    Card previewing the catalogue-on-a-map integration. Included by
    pages/mashup.ejs. Takes no locals: the map fetches its own data. %>
<article aria-labelledby="mashup-1" class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
  <div class="flex items-start gap-4">
    <%# Purpose icon: a coloured chip makes each mashup scannable at a
        glance. Decorative, so the SVG is aria-hidden. %>
    <span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-lg bg-blue-50 text-blue-600">
      <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" />
      </svg>
    </span>
    <div class="min-w-0 flex-1">
      <div class="flex items-start justify-between gap-3">
        <h2 id="mashup-1" class="text-lg font-semibold">Products + Location</h2>
        <span class="shrink-0 rounded-full border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700">Live</span>
      </div>
      <p class="mt-1 text-sm text-slate-600">
        Show customers where products are physically available on an
        interactive map they can pan and zoom.
      </p>
      <p class="mt-2 text-xs font-medium uppercase tracking-wide text-slate-500">
        Combines: LPA catalogue + Leaflet + OpenStreetMap
      </p>
    </div>
  </div>

  <%# Leaflet renders into this container. The explicit height matters: an
      element with no height gives Leaflet nothing to draw into. `isolate`
      and `z-0` keep Leaflet's internal z-indexes from painting over the
      fixed site header. %>
  <div id="mashup-map" role="region" aria-label="Map of stores holding these products"
    class="isolate z-0 mt-5 h-[300px] w-full rounded-lg border border-slate-200 md:h-[400px]"></div>
  <p class="mt-2 text-xs text-slate-500">Map data © OpenStreetMap contributors</p>
</article>
